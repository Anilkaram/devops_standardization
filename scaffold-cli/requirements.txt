# CLI framework and prompt handling
typer>=0.9.0

# Parsing the infra.yaml and rules engine files
PyYAML>=6.0

# Templating engine for generating Terraform files without hardcoding
Jinja2>=3.1.2

# LLM-powered config extraction (optional — needed for --describe flag)
anthropic>=0.40.0